image:
  # To jest kluczowy krok: używamy gotowego, oficjalnego obrazu Anchor
  file: .gitpod.Dockerfile

tasks:
  - init: |
      # Ustawiamy właściwą wersję, jeśli jest inna
      avm use 0.30.1
      # Instalujemy zależności frontendu
      npm install
    command: |
      echo "Środowisko jest w pełni gotowe."
      echo "Sprawdź wersje: solana --version && anchor --version"

vscode:
  extensions:
    - rust-lang.rust-analyzer