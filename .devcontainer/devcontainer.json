{
    "name": "Final Solana Environment",
    "build": {
        "dockerfile": "Dockerfile"
    },
    // Ta sekcja zainstaluje wszystko, czego potrzebujemy, PO zbudowaniu kontenera, co jest bardziej niezawodne.
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y pkg-config build-essential libssl-dev git curl protobuf-compiler && rustup-init -y && echo 'source \"$HOME/.cargo/env\"' >> ~/.bashrc && sh -c \"$(curl -sSfL https://release.solana.com/v1.18.14/install)\" && cargo install avm && /home/vscode/.cargo/bin/avm install 0.30.1 && /home/vscode/.cargo/bin/avm use 0.30.1",
    "customizations": {
        "vscode": {
            "extensions": [
                "rust-lang.rust-analyzer"
            ],
            "settings": {}
        }
    },
    // Uruchom jako root, aby uniknąć wszelkich problemów z uprawnieniami
    "remoteUser": "root"
}